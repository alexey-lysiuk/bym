--- a/smpeg-config.in
+++ b/smpeg-config.in
@@ -50,7 +50,7 @@
       else
         libdirs="-L@libdir@ @SMPEG_RLD_FLAGS@"
       fi
-      echo $libdirs -lsmpeg `@SDL_CONFIG@ --libs`
+      echo $libdirs -lsmpeg `@SDL_CONFIG@ --libs` -lstdc++
       ;;
     *)
       echo "${usage}" 1>&2
